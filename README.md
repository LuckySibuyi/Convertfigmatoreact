
  # Convert Figma to React

  This is a code bundle for Convert Figma to React. The original project is available at https://www.figma.com/design/aHJA53a3pTpzhYhwLX461y/Convert-Figma-to-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  